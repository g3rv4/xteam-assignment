(function (define) {
    define(
        ['lodash'],
        function (_) {
            var MainController = function () {
                var _this = this;

            };

            return [MainController];
        }
    );
}(define));
